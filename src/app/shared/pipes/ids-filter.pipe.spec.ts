import { IdsFilterPipe } from './ids-filter.pipe';

describe('IdsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IdsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
